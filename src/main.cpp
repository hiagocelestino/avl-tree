#include <iostream>
#include "avl.hpp"

using namespace std;
int main() {
    AVL avl = AVL();
    string palavra;
    int pagina;

    while (!feof(stdin)){
        cin >> palavra >> pagina;
    }
    return 0;
}
